{
  "dependencies": {
    "express": "^4.18.0"
  },

"scripts": {
  "start" : "npm run build && node server/index.js",
  "cre-start": "react-scripts start",
  "build": "react-scripts build",
  "test": "react-scripts test",
  "eject": "react-scripts eject"
  }

}